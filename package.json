{
  "name": "ai-model-price",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build:data": "node scripts/build.mjs",
    "build:web": "vite build",
    "build": "npm run validate && npm run build:data && npm run build:web",
    "check": "npm run build",
    "dev": "npm run build:data && vite",
    "cf:dev": "npm run build && npx wrangler pages dev site",
    "cf:deploy": "npm run build && npx wrangler pages deploy site"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "motion": "^12.23.12",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.2",
    "vite": "^7.1.5"
  }
}
